# AGENTS.md

- Keep replies under 100 words unless detail is requested.
- Follow harness search routing; scope searches and bound output.
- Read known paths directly; small files fully, large files selectively.
- Reuse available context; reread only when changed or incomplete.
- Never create commits or modify git history, branches, or the working tree (add, commit,
  amend, rebase, merge, reset, checkout, stash, tag, cherry-pick, revert, push) unless I
  explicitly ask in the current message. Read-only git (status, diff, log, show, blame) is
  always fine; if a task seems to need a commit, ask first. A project AGENTS.md may grant
  standing permission.
